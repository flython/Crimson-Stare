# 2026-08-29

- 完成血色牌局 Web 版的 grilling 需求拷问，三轮定稿，设计共识已沉淀到 MEMORY.md。
- 关键决策：MVP=简易模式+标准局2-4人+单人模式+黑心商贩事件；TS monorepo（engine/server/web）+ WebSocket 服务端权威 + docker-compose；效果硬编码注册表不造DSL；奖励表配置化；暗色UI（#e9404b主色）。
- 里程碑：M1 白板标准局全流程 → M2 黑市+角色+简易模式+事件 → M3 单人荷官。
- 用户待办：M2 前给首批卡牌文本；M3 前确认命运牌文本。
- 黑心商贩事件最终从 MVP 移除（用户改主意）。
- git 仓库初始化，spec.md 按 to-spec 产出并提交；wayfinder 建图：map.md + 7 张票据（01骨架/02引擎抽象/03牌型求解器/04WS协议/05UI原型/06卡牌管道/07部署），frontier=01+03。
- 确立提交规范：中文有序列表 commit msg，已存入项目 MEMORY.md。
- 解决票据01（npm workspaces 骨架：engine/server/web 三包 + vitest + eslint/prettier）与票据03（15级牌型枚举+JOKER枚举赋值求解器，17用例含规则书示例全过）。02/07 号票据解除阻塞，frontier=02、07。

## 票据推进（第二轮）
- 07 docker-compose：双镜像构建+双容器启动+WS反代握手实测通过后提交。期间发现 Docker daemon 未启动（open -a Docker 解决）；修复 engine hand-evaluator 的 TS 闭包收窄 never 报错（best 改对象引用）；注意 npm run build 曾被增量缓存骗过——engine 单独 tsc --noEmit 才是真检查。
- 02 引擎核心抽象（prototype→ready-for-human）：core/state、core/rng（mulberry32重放）、core/effects（EffectDef注册表+来源优先级+特权证顺时针排序）、core/redact（可见性在分发层）、game/whiteboard 纯函数 reducer 跑通 8 阶段；GameConfig 收敛进 engine 单一事实源。25 测试全过。待飞飞拍板后转 resolved。
- 提交规范维持中文有序列表。

## 票据推进（第三轮）
- 02 转 resolved（飞飞"继续"视为确认抽象）。
- 05 UI 原型（ready-for-human）：单文件 table-ui.html，8阶段步进条+各阶段交互形态+宣告三步面板+芯片插入演示；待飞飞体验拍板。
- frontier 剩余：04（WS协议，grilling）、06（卡牌数据管道，grilling）——两票问题已在对话中提出等飞飞回答。

## 票据推进（第四轮）
- 04 WS协议 resolved：契约 docs/protocol.md（8+5消息类型表/快照广播/托管/房间生命周期），飞飞确认全量快照、120s托管、终局写SQLite摘要。
- 06 卡牌管道 resolved：交付模板 config/card-pool-template.xlsx（4张牌类sheet+说明页，列可扩展按列名识别），图片约定 assets/cards/<牌类>/<ID>.png；转换脚本推迟到首批模板返回后写。
- 至此 7 张票据 6 张 resolved，仅 05 UI 原型 ready-for-human 等飞飞体验拍板。
- frontier 已清空。下一步：05 拍板后进入 M1 实装（server 房间/同步 + web 牌桌接真引擎）。

## 05 原型 v2 修订
- 飞飞意见落实：黑市居中+四向座位+各座位独立出牌区（牌面按方位旋转），座位信息牌（角色牌/车票/换牌n/上限/牌库张数），出牌飞行动画，个人操作台（手牌/牌堆弃牌堆张数/道具区）。升级4人局演示。仍 ready-for-human。

## 05 原型 v3 修订
- 飞飞意见落实：阶段推进改程序控制——步进条只读、每阶段"结束阶段"按钮、结束标记同步到各座位（✓已结束）、全员结束自动转场、转场大字动画自动缩入顶栏指示器。仍 ready-for-human。
- 该机制对引擎的含义：advancePhase 需收集全员 endPhase 意愿后才推进，Action 流需加 endPhase 类型，server 层据此广播快照。

## 05 原型 v4 修订
- 飞飞意见：头像undefined（S.me缺name/char，已补）、按钮竖排在提示下（操作台改两行：昵称+按钮 / 提示+进度）、提示字号12→14、965x440挤爆（新增max-height:560px断点）、点头像看技能（charOverlay，演示文本）。仍 ready-for-human。

## 05 票据收口（22:30 左右）
- 飞飞拍板：UI 原型 v4 定为实装基线（"先这样，以后再改"），05 转 resolved。
- UI/交互结论整合进 spec.md 新增「UI 与交互决策」节（四向布局/操作台两行/endPhase 阶段推进/各阶段交互/断点/视觉），实装以 spec 为准。
- 至此 7 张票据全部 resolved，下一步进入 M1 实装：server 房间/同步层 + web 牌桌接真引擎。
- 提交 480ca1a（中文有序列表规范）。

## 卡牌数据提取（23:50 起）
- 从三份扫描件提取：角色牌 21 张（01-21，PDF 共 21 页）、黑市牌 51 张（NO.001-052 缺 013）、命运牌 15 张（F1-F15）、事件牌 36 张（E1-E36）。
- 卡图按类型归档至 assets/cards/{role,market,fate,event}/<ID>.png，角色牌 sheet 已按用户要求删除数量列。
- 数据填入 config/card-pool-template.xlsx 四张 sheet，提交 7b6f078。
- 遗留：简易模式可用 4 角色与黄色边框黑市牌需飞飞确认；角色价格列模板必填但卡面无价格，暂填"?"；部分黑市效果中的资源图标（血滴/车票/皇冠）已按卡面保留为 Unicode/方括号形式。
