# 24 - 规则书核对修复批次

Type: task
Status: resolved

## Question

飞飞对照 `docs/血色牌局_规则书.md` 与卡面文本做了全量核对，发现 21 项偏离，分三组：

### A. 严重规则 / 功能缺陷（本期修复）

| # | 位置 | 问题 | 根因 | 修复 |
|---|---|---|---|---|
| 1 | `effects/market.ts` `pluckChip` | 042「拔除芯片」删除整张牌（含芯片）而非拔除芯片 | resolve 调 `deleteFromDiscard` 把牌从弃牌区移到删牌区 | 仅移除 `chips[cardId]`、从 `baseRank` 还原点数、牌留弃牌区、+4 筹 |
| 2 | `assets/cards/market/037.png` ↔ `042.png` | 文件名互换：037.png 是拔除芯片，042.png 是特权分红 | 命名错位 | `mv` 互换两个文件 |
| 3 | `core/redact.ts` `digestZones` | 出牌区对他人始终下发 `{cards}`，泄露暗扣 | redact 写死 `{ cards: z.play }` | 仅在 `phase !== "play"` 或本人时下发牌面，否则 `{ count }` |
| 4 | `game/whiteboard.ts` `createGame` + `deleteCards` | 简易模式未按规则 10.1 处理牌库与免费删牌 | deck 永远 54 张；free quota 不分模式 | `buildDeck` 加 `simple` 参数去 11-14；GameState 加 `simple`；`deleteCards` 简单模式 `freeQuota=0` |
| 5 | `game/whiteboard.ts` `purchase` / `skipPurchase` | 购买未按规则 5.6 顺时针依次（先持证者） | 未限制 active buyer | 加 `activePurchaseTurn` 门禁；purchase/skip 仅 active 可发 |
| 6 | `game/whiteboard.ts` swap | 最后一次换牌只能弃 1 张 | `maxDiscard = Math.min(3, p.swapLeft)` | 改为 `Math.min(3, hand.length)`（规则 5.2 与剩余次数无关） |
| 7 | `game/whiteboard.ts` `checkVictory` | 终局平局直接取票数最高者并列 | 未比血筹、未比顺时针距离 | 改为 `reached` → 血筹降序 → 顺时针距特权证最近者单胜 |

### B. 卡牌规则偏差与主要体验（本期部分修复）

| # | 位置 | 问题 | 处理 |
|---|---|---|---|
| 8 | `effects/market.ts` `itemEffect` + web 道具区 | 045-052 备用道具无「使用」入口 | 票据 20 已标 TODO；列入 M4「道具使用时机」批次 |
| 9 | `effects/market.ts` `cheapDelete` | 027 未限「至多 2 张」 | 本期：resolve 加 `.slice(0, 2)` |
| 10 | `effects/market.ts` TODO 014/015/016/023/024/035 | 芯片交互未实现 | 票据 20 遗留；列入 M4「芯片交互」批次 |
| 11 | 017「双生镜片」与出牌强制 ≥5 张 | 视为 2 张无法省牌位 | 需飞飞裁决（放不放松下限）；列入待定 |
| 12 | `game/whiteboard.ts` `playCards` | maxPlay 用 `5+six`，漏 `seven` | 本期：`5+six+seven` |
| 13 | 结算 UI 不展示牌型/名次 | duelResult 未下发、未渲染 | 列入 UI 批次（M4） |
| 14 | `CardPicker` 不能空选择 | 026/032/033 等允许跳过的效果受限 | 列入 UI 批次（M4） |
| 15 | 043「再来一批」选栏位无牌面 | CardPicker 收不到 market 区域卡数据 | 列入 UI 批次（M4） |

### C. 交互 / UI 细节

| # | 位置 | 问题 | 处理 |
|---|---|---|---|
| 16 | `game/whiteboard.ts` `createGame` | 特权证用抽牌堆顶比点数（应掷骰） | 本期：`rollDice` 比骰子；改动 rng 序列需更新受影响的测试 |
| 17 | `core/redact.ts` | draw 对本人也摘要化 | 金科玉律 2 规定抽牌堆对本人也不可见，本就正确；清洁工「全牌库」选牌走 card-back 兜底渲染即可。列入 UI 微调 |
| 18 | web 缺少查看弃牌区/删牌区入口 | — | 列入 UI 批次（M4） |
| 19 | `server/room.ts` `bestPlayCards` | 托管出牌含声明芯片时引擎报错 | 本期：托管默认 declarations=`{cardId:"any"}` |
| 20 | `effects/roles.ts` `characterPurchasePrice` | 吉祥物「向上取整」与卡面示例「3→1」冲突 | **需飞飞最终裁定**（待回复后改） |
| 21 | `effects/roles.ts` role:05/08/21 | 特型演员/海盗/黑客 TODO | 票据 20 遗留；列入 M4「角色交互」批次 |

## Notes

- 本期修复集中在 A 组全部 + B 组 #9/#12 + C 组 #16/#19，共 10 项。需新增回归测试随每项修复附上。
- B/C 其余项并非「不做」，而是工作量超出单票据，留 M4 批次。
- 提交规范：每项（或一组同类项）独立 commit，按中文有序列表。
- **需要飞飞回复**：#11（017 牌位下限放宽判断）、#20（吉祥物取整方向）。

## Answer

### 本期完成（10 项，全部附回归测试）

- **A 组 7 项全修**：042 拔除芯片改 `removeChip` 原语（只摘芯片+`baseRank` 还原点数，牌留弃牌区）；037/042 卡图互换；出牌区暗扣 `redact`（他人 play 阶段只见 `{count}`）；简易模式牌库去 11-14（38 张）+ 免费删牌配额 0（`GameState.simple`）；购买顺位门禁 `activePurchaseTurn`（持证者起顺时针，`purchaseFlipped` 翻面）；换牌 `maxDiscard = min(3, hand.length)` 不再绑 swapLeft；终局平局先比血筹再比顺时针距特权证距离。
- **B 组 #9/#12**：027 廉价删除 `.slice(0,2)` 限 2 张；maxPlay 改 `5+six+seven`（hand-evaluator 七条返回额外张数=2）。
- **C 组 #16/#19**：特权证改 `rollDice` 掷骰比大小（走 rngState 可重放，不改牌库）；托管出牌对声明类芯片（008-012）默认提交 `declarations={cardId:"any"}`。

### 测试

engine 167 + card-data 15 + server e2e 10 + web 7 = **199 全绿**；lint 0 errors；build 通过。

### 关键决策与遗留

- e2e 固定 `Math.random=0` → seed=0 → 黑市首槽 009 强化芯片（price=1）确定可买：规则 5.6 后每人每购买阶段至多行动一次，旧测试「连续购买刷槽位」假设失效，改单次购买 + 固定 seed 保证「购买挂起→resolve→挂载」路径确定不飘。
- **待飞飞裁决**：#11 017 双生镜片是否放松出牌下限（本期未改）；#20 吉祥物取整方向（代码 `Math.floor`，规则书文字「向上取整」）。
- **M4 批次**（B/C 其余）：道具 045-052 使用入口、芯片交互 TODO 014/015/016/023/024/035、结算 UI 牌型展示、CardPicker 空选择、043 选栏位牌面、弃牌/删牌区查看入口、角色 05/08/21、#17 draw 摘要化（金科玉律 2 本就正确，仅 UI 微调）。
