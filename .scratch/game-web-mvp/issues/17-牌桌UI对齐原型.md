# 17 - 牌桌 UI 对齐原型 v4（table-ui.html）

Type: task
Status: resolved
Blocked by: 15

## Question

用户反馈「界面存在严重问题，参考 .scratch/game-web-mvp/prototypes/table-ui.html」。现状：Table.tsx（15 号票据的最小可玩版）是纵向列表式布局（对手条 → 黑市横排 → 我的手牌/操作区 → 日志），未按 05 号票据拍板的 v4 原型实现四向实体牌桌，视觉与交互骨架差距大。

## Answer

已按 `prototypes/table-ui.html`（v4 基线）重构 `packages/web/src/components/Table.tsx` 布局并扩展 `styles.css`，只动 `packages/web/src/`。

**布局实现清单**
- **顶栏**：回合信息 + 8 阶段条（`.ph`，当前 `cur` 血红高亮、已完成 `done` 置灰）+ 目标票数（固定映射 2 人 24/3 人 20/4 人 16，对齐 `DEFAULT_GAME_CONFIG.ticketGoals`，config 未下发故前端写死）。
- **实体牌桌 grid**：`.tableWrap > .table` 用 `grid-template-areas: "top top top" / "left center right" / "me me me"`；座位方位按相对座位顺时针映射（2 人→top；3 人 1→左 2→右；4 人 1→左 2→上 3→右）。
- **座位**：`.seatInfo`（charCard 占位角色卡 + 名字/特权证/已翻面/✓已结束徽章 + 票筹/换牌/牌库统计 + 他人手牌背面 `.mini-back` 上限 8 张）+ `.playZone` 出牌区（top 牌 rot180、左 rot90、右 rot-90、me 不旋转）；未就绪玩家座位金框高亮。
- **中心黑市**：5 格 `.bm-card`（defId + subtype + 价格 + 叠加血筹角标），分类边框色暂以 subtype 映射（强化芯片紫/秘密交易红/道具金/其他蓝），购买阶段可点、非购买阶段禁用。
- **我的操作台**：`consoleBar`（名字/徽章 + 阶段操作按钮）+ `hintRow`（提示 + 结束进度）+ `consoleMain`（piles：牌堆/弃牌堆/删牌区/道具区/芯片概览 + handRow 手牌多选）。阶段按钮：swap 换牌/停止换牌、play 出牌、purchase 跳过购买、delete 删除弃牌区（改为复用 CardPicker 弹层选牌，修复 15 号"手牌禁用无法选弃牌"的死逻辑）+ 完成(ready)、reshape 重洗/+2 血筹、draw/duel/settle 自动提示。
- **挂起交互**：PendingPromptBanner/TargetPicker/CardPicker 原样复用（overlay/modal 已是原型弹层风格）；删牌选牌也走 CardPicker。
- **终局**：胜者 + 日志面板（`.logPanel`）；对局中日志为底部小窗（max-height 64px 滚动）。
- **样式**：原型变量（:root 已有同值）与关键样式并入 `styles.css`，含 `@media (max-width:860px)` 与 `@media (max-height:560px)` 断点（触屏 ≥38-44px、压缩座位/黑市/手牌尺寸）；`:has(.table-root)` 使牌桌全屏不滚动。

**验证**
- `cd packages/web && npm run build`（tsc -b + vite build）通过；`npx eslint packages/web/src` 0 error 0 warning。
- 冒烟：esbuild 打包 + react-dom/server 渲染假数据快照，38 项原型骨架类全部命中（topbar/phases/table 四向 grid/seatInfo/handBacks/playZone/bm-card 分类色/console/piles/handRow 等），2 人局方位正确。

**遗留**
- 黑市牌名仍以 defId 展示、分类边框色以 subtype 近似映射（server 未下发卡池元数据 colorTag/分类/牌名，勿改 server，留待元数据下发票据）。
- 角色技能点击查看（charCard title 提示、内容占位）、阶段转场动画（phaseTrans）、出牌飞行/转场动效未实现（票据允许简化，视觉基调一致）。
- 道具仅展示无"使用"按钮（engine 无 useItem Action）。
- swap 阶段手牌按钮未做 ≤3 张上限的前端提示（engine 层校验兜底）。

## Notes

- 原型路径 `.scratch/game-web-mvp/prototypes/table-ui.html`（809 行，假数据，样式/结构即规范）。
- 只改 `packages/web/src/`（Table.tsx 重构 + styles.css 扩展），不碰 engine/server。
- 黑市牌名暂以 defId 展示（server 未下发卡池元数据，遗留已记录）。
