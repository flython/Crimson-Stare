# 29 - 043 再来一批：选栏位显示牌面

Type: task
Status: resolved

## Question

issue 24 #15 / map 20 遗留：043 再来一批（黑市区选 0-2 张回黑市牌堆底→补位→可再购买）需要玩家在**黑市区栏位**选牌。`engine/effects/market.ts:447-499` 已实现 043 真身化（选 0-2 张回堆底），`CardPicker.tsx:22` 的 `CardSource` 已支持 `from:"market"`，但 `web/src/components/Table.tsx` **未把黑市区栏位数据接进 CardPicker**，前端选栏位时收不到牌面（只回退 defId）。

## 待实现

1. **Table.tsx**：043 触发时用 CardPicker 的 `from:"market"` 渲染黑市区 5 个栏位（含 cardPool 元数据 marketById 的牌面/价格/血筹叠加），可多选 0-2 张。
2. 空选/回堆后走 engine 已实现的 resolve 链路（043 选 0 张=不补位直接跳过）。
3. 回归测试：e2e 走 043 选牌路径断言回堆与补位。

## Notes

- 引擎 043 逻辑（map 20）已就绪，本票纯前端接线，预计工作量小。
- 依赖 19 号元数据下发（marketById 查找表）已 resolved。