# 35 - 规则口径注释固化（017 双生镜片 + 吉祥物取整）

Type: task
Status: resolved

## Question

飞飞裁决（2026-09-02 grilling）两条规则口径，均为**只补注释不改行为**：

1. **017 双生镜片**（issue 24 #11）：卡面「对决将此牌视为 2 张」不放松出牌阶段至少 5 张实体牌的下限。**口径**：手牌不足 5 张时选所有手牌出（现状 `playCards` 已 `maxPlay = min(5+six+seven, hand.length)` 天然允许），其余时刻必须恰好 5 张；双生镜片仅影响对决判定计数（该牌按 2 张计入张数/点数），不影响出牌下限。
2. **吉祥物**（issue 24 #20）：`config/cards/roles.json:237` 卡面「价格优惠一半（向上取整）。例 3→1」。**口径**：「优惠一半」的优惠额向上取整 = **价格向下取整**，与代码 `Math.floor(price/2)` 一致，卡面文字与示例无矛盾，文字不用改，现有代码正确。

## 待实现

1. `packages/engine/src/game/whiteboard.ts:613-620` `playCards` 的 `maxPlay` 处补注释固化「017 双生镜片不放松出牌下限」口径。
2. `packages/engine/src/effects/roles.ts:106-108` `characterPurchasePrice` 的 `Math.floor(price/2)` 处补注释固化「优惠额向上取整=价格向下取整」口径。
3. 确认无行为变更，现有测试（engine 167 + e2e 10 + web 7 = 199）全绿。

## Notes

- 纯注释/文档固化，无逻辑改动，风险极低。附一次全量测试确认无回归。