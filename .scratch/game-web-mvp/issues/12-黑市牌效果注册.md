# 12 - M2.3 黑市牌效果注册

Type: task
Status: resolved
Blocked by: 09, 10, 13

## Answer

`packages/engine/src/effects/market.ts` 完成 52 张黑市牌注册（24 黄边全量 + 非黄边尽量），`packages/engine/tests/market.test.ts` 新增 24 用例，现有 51 用例无回归，tsc 通过。

**已实现（黄边）**：
- 强化芯片工厂 `chipInstall`：001-007 数值类（校准器/限流阀，resolve 用 `addPermanentRank` 含 2-14 校验）；008-012/017/019/020 声明类插入挂载（每牌限 1 芯片金科玉律 3、无合法目标弃置金科玉律 7、JOKER 不可插）。
- 血筹镀层（出/夺）021/022 以 `(duel, during)` EffectDef 注册，run 遍历芯片持有者实际生效（出=+2 血筹；夺=挂起选对手夺 1 血筹）。
- 秘密交易：027 廉价删除（挂起选至多 2 张免费删）、031 暴力删除（选目标删抽牌堆顶 3 张）、034 货箱盲掏（取供应堆顶按类型递归结算）、036 对赌协议（掷骰得血筹）。
- 052 荷官证：道具统一工厂 `itemEffect` 存 items（规避 whiteboard 判 "备用道具" 与 JSON subtype="道具" 不一致：注册 run 存 items 即可命中注册表）。
- 037 特权分红、043 再来一批：注册占位 run（保持购买链路，仅记 TODO 日志）。

**已实现（非黄边尽量）**：013 空白模板（无效果插入）、028/029/030 闭店礼（得血筹+跳过购买）、039 鬼手探囊（夺临时特权证）、041 血筹分享、042 拔除芯片、045-051 道具入区。

**占位 TODO（原因）**：
- 008-012/017/019/020 的对决花色-点数声明、双生镜片"视为2张"：生效点在 hand-evaluator 牌型判定，需芯片视图支持（超出本票据文件范围，未改 hand-evaluator）。
- 019/020/037 及 018 加密线路"若[皇冠]"：引擎无皇冠状态字段（state.ts 不可改）。
- 043 再来一批：需选黑市区 slot 的交互（chooseCard 的 from 仅支持 ZoneId）。
- 026/032/033/035/038/040/044：多目标交互或需新状态字段（下回合换牌-2、跳过重整等）。
- 014/015/016/023/024/025：对决/结算声明或需状态扩展。
- 道具"使用时"结算（045-052 全部）：使用时机 M2.4 后。

**遗留**：① 声明类芯片对决生效需 hand-evaluator 芯片视图（独立票据/与 14 UI 联调期处理）；② 皇冠状态建模；③ 道具使用结算（M2.4 使用时机）。

## Question

把 52 张黑市牌效果全部注册进引擎（`packages/engine/src/effects/market.ts`），按类型（强化芯片/秘密交易/备用道具）与触发时机落地，并接入购买阶段处理（购买 → 结算 → 黑市维护）。

## Notes

- 分批：先 24 张黄边（简易模式牌池），再 28 张非黄边。
- 强化芯片：购买后插入弃牌堆一张牌（金科玉律 3/4：每牌限 1、点数 2-14 校验、失败弃置该黑市牌）。
- 秘密交易：购买即结算（多数为 gainChips/删除牌/重洗等自动效果）。
- 备用道具：存入 `items` 区，对决宣告时可主动使用一次（使用时机 M2.4 一并处理）。
- 交互类（暴力删除选对手、餐车投毒选目标、防护屏障取消等）依赖 13 号票据交互机制，先占位。
- 规则文本以 `docs/血色牌局_规则书.md` 与 `config/cards/market.json` 的 effectText 为准。
- 每张黑市牌单测：购买 → 断言结算结果（血筹/牌区/芯片挂载）。
