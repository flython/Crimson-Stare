# 31 - 角色 05 特型演员 / 21 黑客 真身化

Type: task
Status: resolved
Blocked by: 28

## Question

issue 24 #21 / map 20 遗留：

- `effects/roles.ts:130` role:05 特型演员：**开局删除 2 张 2**（需选牌交互，且 setup 时牌堆未构建）——TODO placeholder。
- `effects/roles.ts:131` role:21 黑客：**初始构筑改为从全牌库挑 8 张删除**（需选牌交互，属初始构筑阶段而非 setup）——TODO placeholder。

## 待实现

1. **role:05**：确认「开局删除 2 张 2」的时机——若需在初始构筑阶段选 2 张 2 删除，则挂到构筑阶段交互（选牌范围=初始 8 张中的 2 点牌），复用 CardPicker（依赖 28 空选择语义复核）。
2. **role:21**：初始构筑阶段从全牌库（deck）挑 8 张删除，走 `from:"deck"` 的 CardPicker（金科玉律 13：抽到对手的牌删到对手删牌区）。
3. 注册真身化 + 回归测试。

## Notes

- 05/21 都涉及初始构筑阶段选牌，与 13 号交互挂起（pendingPrompt）机制衔接；需确认构筑阶段是否已支持挂起/交互。
- 21 黑客常驻能力（`freeDeleteExtra=1`）已在 `roles.ts:92` 实现，本票只补初始构筑部分。