# 23 - 自动化集成测试与 CI

Type: task
Status: open
Blocked by: 18, 19, 20, 21, 22

## Question

1. **server WS e2e 扩全**：标准局完整对局（选角→构筑→删牌→黑市→暗扣声明→对决→结算→终局）、芯片插入/声明路径、六七条放宽校验、全模式房间流程（沿用现有 ws-e2e 模式）。
2. **web 组件级测试**（Vitest + Testing Library，不开真浏览器）：卡面图片渲染与回退（18）、芯片角标与详情面板（21）、元数据 store。
3. **CI**：根级 `npm run ci`（lint → cards:check → build → test 串行，任一失败即停）；预留 `.github/workflows/ci.yml`（同步骤，推远端后自动生效）。

## Notes

- 维持 spec 测试决策：核心 seam = engine 公开 API，次级 = WS 消息契约，UI 层不引入 Playwright。
- e2e 断言外部可观察行为（快照状态、广播消息），不 mock 引擎内部。
- 本票据为批次收束票：resolved 后更新 map.md frontier 与 Decisions so far。
