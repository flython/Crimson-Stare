# 27 - 结算阶段牌型与名次展示 UI

Type: task
Status: resolved

## Question

issue 24 #13 / spec US 25：结算阶段应展示各玩家出牌区牌型与排名结果。现状 `web/src/components/Table.tsx` 结算阶段仅提示「奖励已发放，确认后进入购买」，未展示牌型/名次。

引擎结算 `settle` 已产出 duelResult（票/血筹奖励已结算，test 绿），但结果未下发前端展示。

## 待实现

1. **server/web**：把 `duelResult`（各玩家牌型、点数、名次、车票/血筹变化）随 snapshot 下发并渲染（结算日志/排名面板）。
2. **Table.tsx**：结算阶段显示「本局牌型对比 + 名次 + 奖励」面板，参考原型 `prototypes/table-ui.html` 的宣告三步面板收束展示。
3. 回归测试：e2e 断言结算阶段 snapshot 含 duelResult 字段。

## Notes

- 纯 UI 展示，不改引擎结算逻辑；引擎侧仅需确认 duelResult 字段已在 snapshot 中下发（若缺则补 redact 放行）。
- 依赖 22 号宣告面板已落地的牌型展示样式，复用 CardImg/牌型文案。