# 25 - 备用道具使用入口（045–052）

Type: task
Status: resolved

## Question

规则书 5.6：备用道具购买后正面朝上放入道具区（无存放上限），后续可使用一次，使用后背面朝上弃置入「黑市回收站」。spec US 28「把备用道具放入道具区并在之后使用一次」、US 29「秘密交易立即结算不占用道具区」。

现状（issue 24 #8 / map 20 遗留）：
- `effects/market.ts:603` TODO 标注道具"使用时"结算未实现（045 信号干扰器/046 广播喇叭/047 防护屏障/048 魔术橡皮/049 消磁枪/050 皮下密信/051 赌徒虹膜/052 荷官证）。
- 引擎购买时把道具 `run` 存进 `PlayerState.zones.items`，但**没有「使用道具」的 Action 与 resolve 链路**。
- `web/src/components/Table.tsx:676-679` 道具区只渲染卡背/名称+tooltip，无「使用」按钮。

## 待实现

1. **引擎**：新增 `useItem` Action（当前玩家从 items 选 1 使用）→ 触发道具 effect（phase 无关，随用随结）→ 使用后从 items 移除、背面朝上弃入「黑市回收站」。金科玉律 7（购买后必须尽可能使用）只约束购买结算，道具使用时机由玩家自定。
2. **8 张道具 effect 真身化**：045 信号干扰器（swap 阶段后随机弃1抽1）/046 广播喇叭（宣称临时特权证，成败奖惩）/047 防护屏障/048 魔术橡皮/049 消磁枪/050 皮下密信/051 赌徒虹膜/052 荷官证——按规则书 §3.2 与卡面 effectText 注册。
3. **web**：道具区卡片加「使用」按钮 → 发 `useItem`；使用后该格置为牌背（弃入回收站）或移除。
4. 回归测试随每张道具注册附上；确认可绑定一次后二次使用被禁。

## Notes

- 部分道具（信号干扰器/广播喇叭等）需要玩家交互或对手作用，视复杂度拆分：交互类可复用 `pendingPrompt` 挂起机制（票据 13）。
- 本票为 M4 批次首张，打通「道具购买→存入→使用→回收」完整闭环，后续芯片交互（26）依赖其 Action 模式。