# 30 - 弃牌区/删牌区查看入口

Type: task
Status: resolved

## Question

金科玉律 1：自己的弃牌区与删牌区对他人不可见，**自己可随时查看**。spec US 19「查看自己的弃牌区与删牌区，但不能看他人/任何抽牌堆」。

现状 `web/src/components/Table.tsx:665-671` 底部仅显示弃牌堆/删牌区**计数**，无查看弹层；`CardPicker.tsx:17/20` 已支持 `from:"discard"/"deleted"` 的区域名渲染，但缺常驻查看入口。

## 待实现

1. **Table.tsx**：弃牌区/删牌区/道具区卡片增加「查看」交互（点击展开该区域所有牌面，CardImg 渲染），仅自己可见（redact 已保证他人收不到，UI 只渲染自己区域）。
2. 抽牌堆保持只见计数（金科玉律 2 自己也不可见），不开放查看。
3. 回归测试：组件级/手测断言自己可见他人区域为空。

## Notes

- 纯 UI，引擎 redact 已按观察者裁剪；本票只补查看入口。
- 依赖 19 元数据下发（CardImg/marketById）已 resolved。