# 18 - 卡牌图片上图

Type: task
Status: resolved
Blocked by: —

## Answer

图片链路已通，build/lint 通过：

- **资产同步**：新增 `packages/web/scripts/sync-assets.mjs`，web 包 `predev`/`prebuild` 钩子自动把仓库根 `assets/cards/`（123 张 png，唯一事实源）拷贝到 `packages/web/public/cards/`（已入 .gitignore 不入库）；Vite dev 服务器与 dist 构建原样伺服。本地与 docker（WORKDIR /app 相对路径解析）通用；web Dockerfile 增 `COPY assets ./assets`。
- **URL 映射**：元数据 `image` 字段 `assets/cards/<类>/<ID>.png` → 请求 `/cards/<类>/<ID>.png`（`cardImgUrl` helper）。
- **渲染与回退**：Table.tsx 新增 `CardImg` 组件——img 绝对定位铺满卡面，`onError` 直接隐藏，露出下层文字占位（零状态回退，无白屏）。覆盖：黑市 5 格卡面、座位角色卡缩略图；扑克牌手牌无对应图片资产（assets 仅角色/黑市/命运/事件四类），维持花色点数渲染。价格/血筹角标等后置绝对定位元素自然叠于图上。
- 组件级测试（img 渲染与回退断言）归票据 23。

## Question

web 卡面从 config JSON 的 `image` 字段（`assets/cards/<类>/<ID>.png`）加载真实图片并渲染到扑克牌/黑市牌/角色牌卡面上，图片缺失或加载失败时回退现有占位渲染（卡名+分类色块+效果文本），不得白屏或布局抖动。

## Notes

- config/cards/*.json 每条已含 `image` 字段；图片按 ID→文件名约定热替换，无需改数据。
- 图片通过 server 下发的元数据或构建期拷贝进 web 静态资源（Vite `public/` 或 import），二选一以实现最简为准。
- 覆盖卡面位置：手牌、出牌区、黑市 5 格、座位角色牌缩略卡。
- 组件级测试断言：有图渲染 img、无图回退占位。
